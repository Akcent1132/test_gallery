import React from 'react';
import '../styles/empty.scss';

const Empty = () => (
  <div className="empty">
    <h2>Empty</h2>
    <p>Put url of image to input or drop json file by drag n drop </p>
  </div>
);

export default Empty;
